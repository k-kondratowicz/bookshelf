export const API_BASE_URL = 'https://www.googleapis.com/books/v1';

export const API_VOLUME_FIELDS_LITE =
	'kind,totalItems,items(id,kind,volumeInfo(title,authors,publishedDate,imageLinks(thumbnail)))';
